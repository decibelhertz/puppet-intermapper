fixtures:
    repositories:
        stdlib: git://github.com/puppetlabs/puppetlabs-stdlib.git
        staging: git://github.com/nanliu/puppet-staging.git
    symlinks:
        intermapper: "#{source_dir}"
